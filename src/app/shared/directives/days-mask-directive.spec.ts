import { DaysMaskDirective } from './days-mask-directive';

describe('DaysMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new DaysMaskDirective();
    expect(directive).toBeTruthy();
  });
});
